export declare type OptionsType = {
    config: string;
    notifications?: boolean;
    watch?: boolean;
};
export declare type OptionFlagsType = {
    config: {
        flags: Array<string>;
    };
    notifications: {
        flags: Array<string>;
    };
    watch: {
        flags: Array<string>;
    };
};
