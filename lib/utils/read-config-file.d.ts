import { OptionsType } from '../types/options.type';
import { ConfigType } from '../types/config.type';
declare const _default: ({ config }: OptionsType) => Promise<ConfigType | ConfigType[]>;
export default _default;
